apiVersion: v1
kind: Service
metadata:
  name: devops-api-service
  namespace: default
  labels:
    app: devops-api
spec:
  type: LoadBalancer
  selector:
    app: devops-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
